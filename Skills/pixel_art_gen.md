# JRPG Pixel Art Generator
Please generate the images listed below in a **4x4 grid** format.

## Image Requirements
- Text : **None**
- Image Size : Square
- Background Color : Gray background
- Table border: 1px
- Table border Color : Black
- Style :
    - Ultra-detailed miniature pixel art
    - Highly dense pixels (comparable to or 64-bit era)
    - Intricate textures within each icon

## List of Images to Generate (JRPG EQUIP ICON)
{ id: 'crisp_dagger'          , name: 'クリスプダガー'                  , stats: { atk: 120, spd: 25 }          , elements: { water: 30 }                 , recipe: { price: 20000, materials: [{ id: 'mat_crisp_shard', amount: 30 }, { id: 'mat_crisp_core', amount: 5 }] } , ability: { name: 'バブルスプラッシュ', description: '通常攻撃時に 20% の確率で魔法ダメージによる追加攻撃を行います。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.20) { battle._abilityTriggered = true; setTimeout(() => { battle.showActionName(attacker.elementId, 'バブルスプラッシュ', 'text-blue-300', 'border-blue-400/50'); if (!defender.isDead) battle.executeAttack(attacker, defender, battle.party.includes(attacker), { actionName: '追加魔法', damageMultiplier: 1.0, isMagic: true, damageType: 'ability', hideActionName: true }); }, 100); return damage; } return damage; } } },
{ id: 'geode_hammer'          , name: 'ジオードハンマー'                , stats: { atk: 180, spd: 5 }           , elements: { earth: 30 }                 , recipe: { price: 21000, materials: [{ id: 'mat_geode_shell', amount: 30 }, { id: 'mat_geode_heart', amount: 5 }] } , ability: { name: '晶洞の衝撃', description: '通常攻撃時に 20% の確率で敵の防御力を 20% 低下させます。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.20) { battle._abilityTriggered = true; setTimeout(() => { battle.showActionName(attacker.elementId, '晶洞の衝撃', 'text-yellow-600', 'border-yellow-700/50'); defender.stats.def = Math.floor(defender.stats.def * 0.8); battle.showDamage(defender.elementId, 'DEF DOWN', 'text-red-400'); }, 50); return Math.floor(damage * 1.2); } return damage; } } },
{ id: 'snow_frog_bow'        , name: '氷精の弓'                        , stats: { atk: 140, matk: 80, spd: 20 }, elements: { ice: 40 }                   , recipe: { price: 22000, materials: [{ id: 'mat_snow_crystal', amount: 30 }, { id: 'mat_snow_frog_dust', amount: 10 }] } , ability: { name: 'フリーズショット', description: '通常攻撃時に 15% の確率で 1.5 倍のダメージを与え、敵の ATB を半減させます。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.15) { battle._abilityTriggered = true; setTimeout(() => { battle.showActionName(attacker.elementId, 'フリーズショット', 'text-cyan-200', 'border-cyan-400/50'); defender.atb = Math.floor(defender.atb / 2); }, 50); return Math.floor(damage * 1.5); } return damage; } } },
{ id: 'crystalinos_lance'     , name: 'クリスタライノスランス'          , stats: { atk: 160, spd: 20 }          , elements: { earth: 30 }                 , recipe: { price: 23000, materials: [{ id: 'mat_crystalinos_horn', amount: 30 }, { id: 'mat_crystalinos_soul', amount: 5 }] } , ability: { name: '岩砕突き', description: '通常攻撃時に 15% の確率で敵の防御力を無視した一撃を与えます。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.15) { battle._abilityTriggered = true; setTimeout(() => { battle.showActionName(attacker.elementId, '岩砕突き', 'text-orange-300', 'border-orange-500/50'); }, 50); const pureDmg = attacker.stats.atk || 0; return Math.floor(pureDmg * 2.0); } return damage; } } },
{ id: 'garnet_sword'          , name: 'ガーネットの剣'                  , stats: { atk: 160, spd: 15 }          , elements: { fire: 40 }                  , recipe: { price: 24000, materials: [{ id: 'mat_garnet_fang', amount: 20 }, { id: 'mat_garnet_pelt', amount: 15 }] } , ability: { name: 'ブラッドドレイン', description: '通常攻撃時に 15% の確率で与えたダメージの 50% を回復します。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.15) { battle._abilityTriggered = true; setTimeout(() => { battle.showActionName(attacker.elementId, 'ブラッドドレイン', 'text-red-500', 'border-red-700/50'); const healAmount = Math.floor(damage * 0.5); if (healAmount > 0) { if (battle.party.includes(attacker)) { attacker.hp.current = Math.min(attacker.stats.hp || attacker.hp.max, attacker.hp.current + healAmount); } else { attacker.currentHp = Math.min(attacker.maxHp, attacker.currentHp + healAmount); } battle.showDamage(attacker.elementId, '+' + healAmount, 'text-green-400'); if (!document.hidden) battle.renderEntities(); } }, 50); return damage; } return damage; } } },
{ id: 'amber_dagger'          , name: '琥珀の短剣'                      , stats: { atk: 150, matk: 50, spd: 30 }, elements: { earth: 30 }                 , recipe: { price: 25000, materials: [{ id: 'mat_amber_scale', amount: 30 }, { id: 'mat_amber_venom', amount: 10 }] } , ability: { name: 'エンシェントヴェノム', description: '通常攻撃時に 20% の確率で発動。対象が毒状態なら 3.0 倍、そうでなければ 1.5 倍のダメージ。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.20) { battle._abilityTriggered = true; const isPoisoned = defender.activeAilment && defender.activeAilment.type === 'poison'; setTimeout(() => { battle.showActionName(attacker.elementId, '太古の猛毒刃', 'text-purple-400', 'border-purple-600/50'); }, 50); return Math.floor(damage * (isPoisoned ? 3.0 : 1.5)); } return damage; } } },
{ id: 'beryl_axe'             , name: 'ベリルアックス'                  , stats: { atk: 200, spd: 10 }          , elements: { light: 30 }                 , recipe: { price: 26000, materials: [{ id: 'mat_beryl_stone', amount: 30 }, { id: 'mat_beryl_reactor', amount: 5 }] } , ability: { name: '重力断', description: '通常攻撃時に 20% の確率で 2.0 倍のダメージを与えます。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.20) { battle._abilityTriggered = true; setTimeout(() => { battle.showActionName(attacker.elementId, '重力断', 'text-green-300', 'border-green-500/50'); }, 50); return Math.floor(damage * 2.0); } return damage; } } },
{ id: 'jewel_blade'           , name: '金剛石の刃'                      , stats: { atk: 190, spd: 25 }          , elements: { light: 30 }                 , recipe: { price: 27000, materials: [{ id: 'mat_jewel_wing', amount: 30 }, { id: 'mat_jewel_blade', amount: 15 }] } , ability: { name: 'ダイヤモンドカット', description: '通常攻撃時に 10% の確率で敵の防御力を完全に無視し、2.5 倍のダメージを与えます。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.10) { battle._abilityTriggered = true; setTimeout(() => { battle.showActionName(attacker.elementId, 'ダイヤモンドカット', 'text-white', 'border-gray-200/50'); }, 50); const pureDmg = attacker.stats.atk || 0; return Math.floor(pureDmg * 2.5); } return damage; } } },
{ id: 'fluorite_bow'          , name: '幻夢の弓'                        , stats: { atk: 160, matk: 120, spd: 20 }, elements: { dark: 30 }                  , recipe: { price: 28000, materials: [{ id: 'mat_fluorite_powder', amount: 40 }, { id: 'mat_fluorite_wing', amount: 15 }] } , ability: { name: 'イリュージョンショット', description: '通常攻撃時に 20% の確率で物理と魔法の 2 回攻撃を行います。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.20) { battle._abilityTriggered = true; setTimeout(() => { battle.showActionName(attacker.elementId, '幻夢撃', 'text-pink-300', 'border-pink-400/50'); setTimeout(() => { if (!defender.isDead) battle.executeAttack(attacker, defender, battle.party.includes(attacker), { actionName: '魔法追撃', damageMultiplier: 1.0, isMagic: true, damageType: 'ability', hideActionName: true }); }, 100); }, 50); return damage; } return damage; } } },
{ id: 'pyrite_spear'          , name: '愚者の槍'                        , stats: { atk: 220, spd: 10 }          , elements: { thunder: 40 }               , recipe: { price: 29000, materials: [{ id: 'mat_pyrite_scale', amount: 30 }, { id: 'mat_pyrite_fang', amount: 15 }] } , ability: { name: 'フールズストライク', description: '攻撃時、10% の確率で 5.0 倍のダメージを与えますが、10% の確率でダメージが 0 になります。', execute: (attacker, defender, damage, battle) => { const rand = Math.random(); if (rand < 0.10) { battle._abilityTriggered = true; setTimeout(() => { battle.showActionName(attacker.elementId, '大当り！', 'text-yellow-400', 'border-yellow-500/50'); }, 50); return Math.floor(damage * 5.0); } else if (rand < 0.20) { battle._abilityTriggered = true; setTimeout(() => { battle.showActionName(attacker.elementId, 'ハズレ...', 'text-gray-500', 'border-gray-600/50'); }, 50); return 0; } return damage; } } },
{ id: 'luminous_staff'        , name: '光樹の杖'                        , stats: { atk: 80, matk: 240, spd: 15 }, elements: { light: 50 }                 , recipe: { price: 32000, materials: [{ id: 'mat_luminous_leaf', amount: 40 }, { id: 'mat_luminous_sap', amount: 20 }] } , ability: { name: 'ライトバースト', description: '通常攻撃時に 15% の確率で 1.5 倍の魔法ダメージを与え、味方全体を与ダメージの 10% 分回復します。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.15) { battle._abilityTriggered = true; const magicDmg = Math.max(1, (attacker.stats.matk || 0) - Math.floor((defender.stats.mdef || 0) / 2)); const finalDmg = Math.floor(magicDmg * 1.5); setTimeout(() => { battle.showActionName(attacker.elementId, 'ライトバースト', 'text-yellow-200', 'border-yellow-400/50'); const healAmount = Math.floor(finalDmg * 0.1); if (healAmount > 0) { const allies = battle.party.includes(attacker) ? battle.party.filter(p=>!p.isDead) : battle.enemies.filter(e=>!e.isDead); allies.forEach(ally => { if (battle.party.includes(ally)) { ally.hp.current = Math.min(ally.stats.hp || ally.hp.max, ally.hp.current + healAmount); } else { ally.currentHp = Math.min(ally.maxHp, ally.currentHp + healAmount); } battle.showDamage(ally.elementId, '+' + healAmount, 'text-green-400'); }); if (!document.hidden) battle.renderEntities(); } }, 50); return finalDmg; } return damage; } } },
{ id: 'opal_wand'             , name: '遊色の杖'                        , stats: { atk: 90, matk: 260, spd: 20 }, elements: {}                            , recipe: { price: 35000, materials: [{ id: 'mat_opal_dust', amount: 40 }, { id: 'mat_opal_fragment', amount: 20 }] } , ability: { name: 'プリズマティックレイ', description: '通常攻撃時に 20% の確率で敵の魔法防御力を無視した虹色の魔法ダメージを与えます。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.20) { battle._abilityTriggered = true; setTimeout(() => { battle.showActionName(attacker.elementId, '虹色の光線', 'text-purple-300', 'border-pink-400/50'); }, 50); return Math.floor(attacker.stats.matk || 0); } return damage; } } },
{ id: 'diamond_greatsword'    , name: 'ダイヤモンドグレートソード'      , stats: { atk: 350, spd: 5 }           , elements: { light: 40 }                 , recipe: { price: 50000, materials: [{ id: 'mat_diamond_scale', amount: 40 }, { id: 'mat_diamond_claw', amount: 10 }] } , ability: { name: '絶対の斬撃', description: '通常攻撃時に 10% の確率で 3.0 倍のダメージを与え、敵の ATB を 0 にします。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.10) { battle._abilityTriggered = true; setTimeout(() => { battle.showActionName(attacker.elementId, '絶対の斬撃', 'text-white', 'border-cyan-200/50'); defender.atb = 0; }, 50); return Math.floor(damage * 3.0); } return damage; } } },
{ id: 'regalia_sword'         , name: '王権の剣'                        , stats: { atk: 280, matk: 200, spd: 25}, elements: { light: 50 }                 , recipe: { price: 55000, materials: [{ id: 'mat_regalia_shard', amount: 40 }, { id: 'mat_regalia_crown', amount: 10 }] } , ability: { name: '王の号令', description: '通常攻撃時に 15% の確率で 3 回の連続攻撃（各 1.2 倍ダメージ）を行います。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.15) { battle._abilityTriggered = true; setTimeout(() => { battle.showActionName(attacker.elementId, '王の号令', 'text-yellow-400', 'border-yellow-600/50'); for (let i=0; i<2; i++) { setTimeout(() => { if (!defender.isDead) battle.executeAttack(attacker, defender, battle.party.includes(attacker), { actionName: '連撃', damageMultiplier: 1.2, damageType: 'ability', hideActionName: true }); }, (i+1)*100); } }, 50); return Math.floor(damage * 1.2); } return damage; } } },
{ id: 'genesis_staff'         , name: 'ジェネシススタッフ'              , stats: { atk: 150, matk: 400, spd: 30}, elements: { light: 60, dark: 60 }       , recipe: { price: 80000, materials: [{ id: 'mat_genesis_fragment', amount: 50 }, { id: 'mat_genesis_core', amount: 10 }] } , ability: { name: '万象の閃光', description: '通常攻撃時に 10% の確率で 4.0 倍の究極魔法ダメージを与えます。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.10) { battle._abilityTriggered = true; setTimeout(() => { battle.showActionName(attacker.elementId, '万象の閃光', 'text-white', 'border-white/80'); }, 50); const magicDmg = Math.max(1, (attacker.stats.matk || 0) - Math.floor((defender.stats.mdef || 0) / 2)); return Math.floor(magicDmg * 4.0); } return damage; } } },