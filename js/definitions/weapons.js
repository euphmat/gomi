/**
 * このファイルは武器（右手に装備するアイテム）のデータをまとめたファイルです。
 * 
 * 武器のデータ定義ファイル
 * 
 * テンプレート（各項目は省略可能、省略された値は 0 として扱われます）
 *    stats:         { hp: 0, mp: 0, atk: 0, def: 0, matk: 0, mdef: 0, spd: 0 },
 *    elements:      { fire: 0, water: 0, grass: 0, ice: 0, thunder: 0, wind: 0, earth: 0, light: 0, dark: 0 },
 *    ailments:      { poison: 0, burn: 0, paralysis: 0, sleep: 0, confusion: 0, curse: 0, blind: 0, silence: 0 },
 */

export const WEAPONS = [
  { id: 'wooden_stick',            name: '木の棒',                            stats: { atk:  1 },                                                                                                                                              ability: { name: '振り下ろし', description: '通常攻撃時に 10% の確率で 2 倍のダメージを与えます。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.10) { battle._abilityTriggered = true;  setTimeout(() => { battle.showActionName(attacker.elementId, '振り下ろし', 'text-yellow-300', 'border-yellow-500/50'); }, 300); return damage * 2; } return damage; } } },
  { id: 'slime_blue_sword',        name: 'ブルースライムソード',              stats: { atk:  6, matk:  2 },           recipe: { price:  100, materials: [{ id: 'slime_blue_jelly',       amount: 5 }, { id: 'slime_blue_core',       amount: 1 }] }, ability: { name: 'スラッシュ', description: '通常攻撃時に 20% の確率で 1.5 倍のダメージを与えます。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.20) { battle._abilityTriggered = true;  setTimeout(() => { battle.showActionName(attacker.elementId, 'スラッシュ', 'text-blue-300', 'border-blue-500/50'); }, 300); return Math.floor(damage * 1.5); } return damage; } } },
  { id: 'slime_green_sword',       name: 'グリーンスライムソード',            stats: { atk: 12, matk:  4 },           recipe: { price:  200, materials: [{ id: 'slime_green_jelly',      amount: 5 }, { id: 'slime_green_core',      amount: 1 }] }, ability: { name: '連続斬り', description: '通常攻撃時に 15% の確率で追加攻撃を行います。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.15) { battle._abilityTriggered = true;  setTimeout(() => { battle.showActionName(attacker.elementId, '連続斬り', 'text-green-300', 'border-green-500/50'); battle.executeAttack(attacker, defender, battle.party.includes(attacker), { actionName: '追撃', damageMultiplier: 0.8, damageType: 'ability', hideActionName: true }); }, 400); } return damage; } } },
  { id: 'slime_red_sword',         name: 'レッドスライムソード',              stats: { atk: 18, matk:  6 },           recipe: { price:  300, materials: [{ id: 'slime_red_jelly',        amount: 5 }, { id: 'slime_red_core',        amount: 1 }] }, ability: { name: '渾身の斬撃', description: '通常攻撃時に 10% の確率で 2.5 倍のダメージを与えます。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.10) { battle._abilityTriggered = true;  setTimeout(() => { battle.showActionName(attacker.elementId, '渾身の斬撃', 'text-red-300', 'border-red-500/50'); }, 300); return Math.floor(damage * 2.5); } return damage; } } },
  { id: 'slime_water_staff',       name: 'ウォータースライムスタッフ',        stats: { atk:  8, matk: 24 },           recipe: { price:  400, materials: [{ id: 'slime_water_jelly',      amount: 5 }, { id: 'slime_water_core',      amount: 1 }] }, ability: { name: 'ウォーターボルト', description: '通常攻撃時に 20% の確率で matk に依存した魔法ダメージを与えます。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.20) { battle._abilityTriggered = true;  setTimeout(() => { battle.showActionName(attacker.elementId, 'ウォーターボルト', 'text-blue-400', 'border-blue-500/50'); }, 300); const magicDmg = Math.max(1, (attacker.stats.matk || 0) - Math.floor((defender.stats.mdef || 0) / 2)); return Math.floor(magicDmg * 1.5); } return damage; } } },
  { id: 'slime_fire_sword',        name: 'ファイヤースライムソード',          stats: { atk: 30, matk: 10 },           recipe: { price:  500, materials: [{ id: 'slime_fire_jelly',       amount: 5 }, { id: 'slime_fire_core',       amount: 1 }] }, ability: { name: '爆炎斬り', description: '通常攻撃時に 12% の確率で周囲の敵にもダメージを与えます。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.12) { battle._abilityTriggered = true;  setTimeout(() => { battle.showActionName(attacker.elementId, '爆炎斬り', 'text-orange-400', 'border-orange-500/50'); const targets = battle.party.includes(attacker) ? battle.enemies.filter(e => !e.isDead && e !== defender) : battle.party.filter(p => !p.isDead && p !== defender); targets.forEach((t, i) => { setTimeout(() => { if (!t.isDead) battle.executeAttack(attacker, t, battle.party.includes(attacker), { actionName: '爆炎', damageMultiplier: 0.5, damageType: 'ability', hideActionName: true }); }, i * 150 + 200); }); }, 300); return Math.floor(damage * 1.2); } return damage; } } },
  { id: 'slime_ice_bow',           name: 'アイススライムボウ',                stats: { atk: 24, matk: 24, spd:  6 },  recipe: { price:  600, materials: [{ id: 'slime_ice_jelly',        amount: 5 }, { id: 'slime_ice_core',        amount: 1 }] }, ability: { name: '凍結矢', description: '通常攻撃時に 20% の確率で atk と matk を合わせたダメージを与えます。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.20) { battle._abilityTriggered = true;  setTimeout(() => { battle.showActionName(attacker.elementId, '凍結矢', 'text-cyan-300', 'border-cyan-500/50'); }, 300); const hybridDmg = Math.max(1, ((attacker.stats.atk || 0) + (attacker.stats.matk || 0)) - Math.floor((defender.stats.def || 0) / 2)); return Math.floor(hybridDmg * 1.2); } return damage; } } },
  { id: 'slime_wind_sword',        name: 'ウインドスライムソード',            stats: { atk: 42, matk: 14 },           recipe: { price:  700, materials: [{ id: 'slime_wind_jelly',       amount: 5 }, { id: 'slime_wind_core',       amount: 1 }] }, ability: { name: '真空波', description: '通常攻撃時に 10% の確率で敵全体にダメージを与えます。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.10) { battle._abilityTriggered = true;  setTimeout(() => { battle.showActionName(attacker.elementId, '真空波', 'text-teal-300', 'border-teal-500/50'); const targets = battle.party.includes(attacker) ? battle.enemies.filter(e => !e.isDead && e !== defender) : battle.party.filter(p => !p.isDead && p !== defender); targets.forEach((t, i) => { setTimeout(() => { if (!t.isDead) battle.executeAttack(attacker, t, battle.party.includes(attacker), { actionName: '真空波', damageMultiplier: 0.6, damageType: 'ability', hideActionName: true }); }, i * 100 + 100); }); }, 300); return Math.floor(damage * 1.2); } return damage; } } },
  { id: 'slime_thunder_bow',       name: 'サンダースライムボウ',              stats: { atk: 32, matk: 32, spd:  8 },  recipe: { price:  800, materials: [{ id: 'slime_thunder_jelly',    amount: 5 }, { id: 'slime_thunder_core',    amount: 1 }] }, ability: { name: '雷の連矢', description: '通常攻撃時に 10% の確率で atk と matk に依存する 2～4 回のランダムな連続攻撃を行います。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.10) { battle._abilityTriggered = true;  const hits = Math.floor(Math.random() * 3) + 2; setTimeout(() => { battle.showActionName(attacker.elementId, '雷の連矢', 'text-yellow-400', 'border-yellow-600/50'); for(let i=0; i<hits; i++) { setTimeout(() => { const targets = battle.party.includes(attacker) ? battle.enemies.filter(e=>!e.isDead) : battle.party.filter(p=>!p.isDead); const target = targets[Math.floor(Math.random() * targets.length)]; if (target) { battle.executeAttack(attacker, target, battle.party.includes(attacker), { actionName: '落雷', damageMultiplier: 0.5, damageType: 'ability', hideActionName: true }); } }, i * 150 + 200); } }, 300); const hybridDmg = Math.max(1, ((attacker.stats.atk || 0) + (attacker.stats.matk || 0)) - Math.floor((defender.stats.def || 0) / 2)); return Math.floor(hybridDmg * 0.8); } return damage; } } },
  { id: 'slime_flower_bow',        name: 'フラワースライムボウ',              stats: { atk: 36, matk: 36, spd:  9 },  recipe: { price:  900, materials: [{ id: 'slime_flower_jelly',     amount: 5 }, { id: 'slime_flower_core',     amount: 1 }] }, ability: { name: 'フラワーシャワー', description: '通常攻撃時に 12% の確率で敵全体に atk と matk に依存するダメージを与えます。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.12) { battle._abilityTriggered = true;  setTimeout(() => { battle.showActionName(attacker.elementId, 'フラワーシャワー', 'text-pink-300', 'border-pink-500/50'); const targets = battle.party.includes(attacker) ? battle.enemies.filter(e=>!e.isDead && e!==defender) : battle.party.filter(p=>!p.isDead && p!==defender); targets.forEach((t, i) => { setTimeout(() => { if (!t.isDead) battle.executeAttack(attacker, t, battle.party.includes(attacker), { actionName: '花びら', damageMultiplier: 0.7, damageType: 'ability', hideActionName: true }); }, i * 100 + 200); }); }, 300); const hybridDmg = Math.max(1, ((attacker.stats.atk || 0) + (attacker.stats.matk || 0)) - Math.floor((defender.stats.def || 0) / 2)); return Math.floor(hybridDmg * 1.0); } return damage; } } },
  { id: 'slime_grass_staff',       name: 'グラススライムスタッフ',            stats: { atk: 20, matk: 60 },           recipe: { price: 1000, materials: [{ id: 'slime_grass_jelly',      amount: 5 }, { id: 'slime_grass_core',      amount: 1 }] }, ability: { name: 'ウィップバインド', description: '通常攻撃時に 15% の確率で matk 依存の連続攻撃を行います。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.15) { battle._abilityTriggered = true;  setTimeout(() => { battle.showActionName(attacker.elementId, 'ウィップバインド', 'text-green-400', 'border-green-500/50'); battle.executeAttack(attacker, defender, battle.party.includes(attacker), { actionName: '追撃', isMagic: true, damageMultiplier: 1.0, damageType: 'ability', hideActionName: true }); }, 400); const magicDmg = Math.max(1, (attacker.stats.matk || 0) - Math.floor((defender.stats.mdef || 0) / 2)); return Math.floor(magicDmg * 1.0); } return damage; } } },
  { id: 'slime_dark_bow',          name: 'ダークスライムボウ',                stats: { atk: 44, matk: 44, spd: 11 },  recipe: { price: 1100, materials: [{ id: 'slime_dark_jelly',       amount: 5 }, { id: 'slime_dark_core',       amount: 1 }] }, ability: { name: 'ダークピアッサー', description: '通常攻撃時に 8% の確率で atk と matk に依存する強力な防御無視ダメージを与えます。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.08) { battle._abilityTriggered = true;  setTimeout(() => { battle.showActionName(attacker.elementId, 'ダークピアッサー', 'text-purple-600', 'border-purple-800/50'); }, 300); const hybridDmg = (attacker.stats.atk || 0) + (attacker.stats.matk || 0); return Math.floor(hybridDmg * 1.5); } return damage; } } },
  { id: 'slime_earth_staff',       name: 'アーススライムスタッフ',            stats: { atk: 24, matk: 72 },           recipe: { price: 1200, materials: [{ id: 'slime_earth_jelly',      amount: 5 }, { id: 'slime_earth_core',      amount: 1 }] }, ability: { name: 'アースクエイク', description: '通常攻撃時に 10% の確率で敵全体に matk 依存のダメージを与えます。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.10) { battle._abilityTriggered = true;  setTimeout(() => { battle.showActionName(attacker.elementId, 'アースクエイク', 'text-yellow-600', 'border-yellow-700/50'); const targets = battle.party.includes(attacker) ? battle.enemies.filter(e => !e.isDead && e !== defender) : battle.party.filter(p => !p.isDead && p !== defender); targets.forEach((t, i) => { setTimeout(() => { if (!t.isDead) battle.executeAttack(attacker, t, battle.party.includes(attacker), { actionName: '地割れ', isMagic: true, damageMultiplier: 0.8, damageType: 'ability', hideActionName: true }); }, 200); }); }, 300); const magicDmg = Math.max(1, (attacker.stats.matk || 0) - Math.floor((defender.stats.mdef || 0) / 2)); return Math.floor(magicDmg * 1.2); } return damage; } } },
  { id: 'slime_angel_bow',         name: 'エンジェルスライムボウ',            stats: { atk: 52, matk: 52, spd: 13 },  recipe: { price: 1300, materials: [{ id: 'slime_angel_jelly',      amount: 5 }, { id: 'slime_angel_core',      amount: 1 }] }, ability: { name: 'エンジェルアロー', description: '通常攻撃時に 5% の確率で atk と matk を合わせた大ダメージを与え、味方全体を回復します。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.05) { battle._abilityTriggered = true;  setTimeout(() => { battle.showActionName(attacker.elementId, 'エンジェルアロー', 'text-yellow-100', 'border-yellow-300/50'); const healAmount = Math.floor(((attacker.stats.atk || 0) + (attacker.stats.matk || 0)) * 0.3); const allies = battle.party.includes(attacker) ? battle.party.filter(p=>!p.isDead) : battle.enemies.filter(e=>!e.isDead); allies.forEach(ally => { if (battle.party.includes(ally)) { ally.currentHp = Math.min(ally.hp.max, ally.currentHp + healAmount); } else { ally.hp.current = Math.min(ally.hp.max, ally.hp.current + healAmount); } battle.showDamage(ally.elementId, '+' + healAmount, 'text-green-400'); }); }, 300); const hybridDmg = Math.max(1, ((attacker.stats.atk || 0) + (attacker.stats.matk || 0)) - Math.floor((defender.stats.def || 0) / 2)); return Math.floor(hybridDmg * 2.0); } return damage; } } },
  { id: 'slime_king_staff',        name: 'キングスライムスタッフ',            stats: { atk: 28, matk: 84 },           recipe: { price: 1400, materials: [{ id: 'slime_king_jelly',       amount: 5 }, { id: 'slime_king_core',       amount: 1 }] }, ability: { name: 'キングスマッシュ', description: '通常攻撃時に 8% の確率で、matk に依存した絶大なダメージを与えます。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.08) { battle._abilityTriggered = true;  setTimeout(() => { battle.showActionName(attacker.elementId, 'キングスマッシュ', 'text-purple-400', 'border-purple-500/50'); }, 300); const magicDmg = Math.max(1, (attacker.stats.matk || 0) - Math.floor((defender.stats.mdef || 0) / 2)); return Math.floor(magicDmg * 3.0); } return damage; } } },
  { id: 'slime_angel_king_staff',  name: 'エンジェルキングスライムスタッフ',  stats: { atk: 30, matk: 90 },           recipe: { price: 1500, materials: [{ id: 'slime_angel_king_jelly', amount: 5 }, { id: 'slime_angel_king_core', amount: 1 }] }, ability: { name: 'ホーリーレイ', description: '通常攻撃時に 5% の確率で敵全体に matk 依存の光属性ダメージを与え、味方全体のHPを回復します。', execute: (attacker, defender, damage, battle) => { if (Math.random() < 0.05) { battle._abilityTriggered = true;  setTimeout(() => { battle.showActionName(attacker.elementId, 'ホーリーレイ', 'text-yellow-200', 'border-yellow-400/50'); const healAmount = Math.floor((attacker.stats.matk || 0) * 0.5); const allies = battle.party.includes(attacker) ? battle.party.filter(p=>!p.isDead) : battle.enemies.filter(e=>!e.isDead); allies.forEach(ally => { if (battle.party.includes(ally)) { ally.currentHp = Math.min(ally.hp.max, ally.currentHp + healAmount); } else { ally.hp.current = Math.min(ally.hp.max, ally.hp.current + healAmount); } battle.showDamage(ally.elementId, '+' + healAmount, 'text-green-400'); }); const targets = battle.party.includes(attacker) ? battle.enemies.filter(e => !e.isDead && e !== defender) : battle.party.filter(p => !p.isDead && p !== defender); targets.forEach((t, i) => { setTimeout(() => { if (!t.isDead) battle.executeAttack(attacker, t, battle.party.includes(attacker), { actionName: '光の雨', isMagic: true, damageMultiplier: 1.0, damageType: 'ability', hideActionName: true }); }, i * 100 + 200); }); }, 300); const magicDmg = Math.max(1, (attacker.stats.matk || 0) - Math.floor((defender.stats.mdef || 0) / 2)); return Math.floor(magicDmg * 1.5); } return damage; } } },
].map(item => ({ ...item, slot: 'rightHand', image: `./assets/weapon/${item.id}.webp` }));
